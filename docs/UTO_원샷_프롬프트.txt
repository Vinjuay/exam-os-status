너는 UTO 구축 실행 세션이다. 두 단계로 움직인다. **1단계 = 검증만. 2단계 = 내가 「시작」이라고 쓴 뒤에만.** 「시작」 전에는 파일 하나도 만들거나 고치지 마라.

## 1단계 — 검증 (읽기만)

아래를 실측해 표 하나로 보여주고 멈춰라. 없는 것은 「없음」, 못 잰 것은 「측정 불가」로 쓴다. 추정 금지.

| 항목 | 확인 방법 |
|---|---|
| `C:\클로드\클로드 코드\docs\` 안의 `UTO_통합설계_v1.0.md` · `UTO_설계변경_v1.1.md` · **`UTO_설계변경_v1.2.md` · `UTO_구축_실행지시서_v2.1.md`** 각각 존재 여부 | dir. v1.2·v2.1이 없으면 설계보강 세션이 안 돈 것이다 — 그 사실만 보고하고 2단계 진입 금지 |
| `C:\uto\` 내용물 | dir (09-13엔 `tmp\`뿐이었다) |
| `run.bat`의 `test` 인자 · `scripts\selftest.py` · 현재 통과 수 | `run.bat test` 1회 실행 |
| `config.json`의 `spec_ref` · `databases` · `params.assignment_plan` 값 | 그대로 인용 |
| ERRORLOG·국어의 「영역」 속성 **실제 이름과 타입** | 노션 fetch |
| 등록된 스케줄러 작업 **전부** — 이름·다음 실행 시각·상태 | `schtasks /Query /FO LIST` — 구 `ExamOS_*`가 있으면 따로 표시 |
| 이 세션에 노션 MCP가 있는가 · **쓰기가 되는가** | 대기함 페이지에 공백 1줄 append 후 즉시 제거로 실측. 승인창이 뜨거나 거부되면 그 사실을 STATE-LOG에 한 줄 남겨라 |
| 이 세션에 스킬 발행 도구가 있는가 | 도구 목록 |
| `git status` 클린 여부 · 최근 커밋 | git |
| `C:\` 여유 공간 · 저장소 크기(`_backup_*` 포함) | 복사 이전에 필요 |
| 프로젝트 파일 3개(세지 스키마·지도 빈도표·아이패드 자동화)가 노션 LIBRARY·📜 로그에 이관돼 있는가 | 노션 검색. 없으면 P0-1 ④ 전에 이관이 먼저다 |
| `home.py`·`dashboard.py`·`watch.py`를 부르는 곳 전부 | grep — bat·스케줄러 XML·hooks |

표 아래에 두 가지를 덧붙여라:
- **아래 2단계 프롬프트와 실제가 다른 곳** 전부 — 경로·파일명·작업명·속성명·시각
- **되돌릴 수 없는 동작**이 숨어 있는 줄 전부 — 프로젝트 파일 삭제 · ALTER · 스케줄러 변경 · 덮어쓰기

그리고 멈춰라. 내가 「시작」이라고 쓰면 2단계로 간다. 검증 표에 「없음」이 하나라도 있으면 그 블록은 시작해도 건너뛰고 이유를 남긴다.

---

## 2단계 — 구축 (「시작」 이후)

### 먼저 읽어라
1. 노션 STATE (턴 첫 행동)
2. `docs\UTO_설계변경_v1.2.md` — §0 경고 · §1 14건 판정 · §2 채택 설계 · §4 확정 결정
3. `docs\UTO_구축_실행지시서_v2.1.md` — 블록 규약과 수락 기준
4. `docs\UTO_설계변경_v1.1.md` §1 — 복습 저장함
5. `docs\UTO_통합설계_v1.0.md` — B·C·D·M만

**`docs\`의 위치**: P0-2 전까지는 `C:\클로드\클로드 코드\docs\`, P0-2 뒤로는 `C:\uto\docs\`.

### 어겨선 안 되는 것
- **삭제 금지.** 정리는 이동, 이전은 복사. 유일한 예외는 P0-1 ④ 프로젝트 파일이며 그것도 이관 확인 뒤에만.
- **지어내지 마라.** 도구가 없으면 「없음」, 못 잰 값은 「측정 불가」.
- **`run.bat test` 통과 수가 줄면 멈추고 보고해라.**
- 되돌릴 수 없는 작업 전에는 멈추고 물어라.
- Anki 카드 생성·병합·이미지·게이트 개방·태그 **전면 보류**(우뢰 09-16).
- 규칙 문장·저장함 항목·Anki GUID·시간표 시각 불변.
- **노션 가짜 데이터 금지.** 모든 자체 검증은 sqlite 임시 파일에서만.

### 진행 방식
블록마다: **작업 → `run.bat test` → 수락 대조 → 한 줄 보고 → 백업 폴더 + git 커밋 → `data\build_progress.json`에 `{블록, 완료시각, 통과수}` 기록 → 다음.**
재개 시 이 파일을 먼저 읽고 완료된 블록은 수락 기준만 재확인한다. 수락을 못 채우면 넘어가지 말고 막힌 곳을 보고해라.

---

### P0-1 · 청소 (작업 폴더 `C:\클로드\클로드 코드`)
① `_archive_0919\` 생성 후 `_backup_*` 전부 · `v25_patch\` · `_retired_v1\` · `_artifacts_09*` 이동
② `logs\`에서 `_a09*`·`_tmp*`·`_ex*`·`_fake*`·`_conf*`만 `logs\_archive\`로. `.jsonl`·`.log`·`state.json`은 그대로
③ `scripts\statelog_wrapper_09*.py` 16개 중 실제 import·호출되는 것을 grep으로 판정해 하나만 남기고 이동. 근거 출력
④ **1단계에서 이관 확인이 된 경우에만** 나를 불러 프로젝트 파일 삭제 절차를 불러줘라. 이관이 안 돼 있으면 먼저 LIBRARY·📜 로그에 이관하고 노션 URL을 보여준 뒤 불러라. 내가 끝냈다고 답할 때까지 기다려라
**수락**: 현 위치 잔여 0 · 원장 파일 제자리 · 통과 수 불변

### P0-2 · 이사 (이 블록 뒤로 작업 폴더 `C:\uto`)
**착수 조건**: 구 스케줄러 작업이 실행 중이면 끝날 때까지 대기(`schtasks /Query`로 상태 확인).
① `git status` 클린 확인 → `C:\클로드\클로드 코드` → `C:\uto` **복사**(`_archive_0919\` 제외)
② 절대경로 치환 — `config.json`·`manifest.json`·`run.bat`·`task-*.bat`·`CLAUDE.md`·`.mcp.json`·`.claude\settings.json`·`.claude\hooks\*`·`scripts\*.py`·스케줄러 XML. 전후 grep 둘 다 출력
③ **구 `ExamOS_*` 작업 전부 `schtasks /Change /DISABLE`** (삭제 아님). 되돌리기 명령 = `/ENABLE`, 적어 둬라
④ 신규 6종 등록 — `UTO_01_야간` 23:30 · `UTO_01b_AI` 23:45(실패 무시) · `UTO_02_아침` 06:40 매일 · `UTO_03_주간` 토 18:30 · `UTO_04_틱` 15분 09:00–22:00 · `UTO_05_인증` 18:00 — 시작 위치 전부 `C:\uto`
⑤ 6종 수동 실행, rc와 로그 출력. **`UTO_04_틱`은 P1-4 전까지 비활성**으로 둔다(orch가 아직 없다)
⑥ 구 폴더에 `README_MOVED.txt` 한 줄
⑦ **여기서 나를 불러라** — UAC 승인과 스케줄러 확인
**수락**: 구 경로 grep 잔여 0 · `schtasks /Query` 신규 6종 + 구 3종 비활성 · 수동 실행 rc=0 · 통과 수 불변
**되돌리기**: 구 3종 `/ENABLE` + 신규 6종 `/DISABLE`. 그 명령 두 줄을 출력해 둬라

### P0-3 · 이름표 (P0-2와 같은 창)
① `config.spec_ref`·`spec\` 두 파일 헤더·마운트 스킬 헤더를 규격 **rev.m** / 시간표 **rev.d**로 통일
② `selftest.py`에 세 표면 rev 일치 단언 추가 — 불일치면 실패
③ 스킬 발행 도구가 1단계에서 「없음」이면 마운트 스킬 헤더는 못 고친다 — 그 한 줄을 대기함에 평문으로
**수락**: 세 표면 일치(스킬 제외 시 그 사실 명시) · 통과 수 +1

### P0-4 · 사용량·복구·종료
① 오늘 소비한 5시간 창 수를 HEARTBEAT에 한 줄. 못 재면 「측정 불가」
② `config.params.degrade_order` = 배정표 > 아침 렌더 > 워커 > 채팅
③ `_backup_*` 14일 경과분을 **`_archive_0919\`로 이동**하는 스크립트. 삭제 아님. 예약 등록 안 함
④ 2026-11-20 발동 종료 체크리스트 파일 1개(실행 안 함)
⑤ 규격에 한 줄 — 「스케줄러·트리거·예약 세션 변경은 노트북 세션에서만」

### P1-1 · 상태표와 DB 7종
① `data\state.sqlite`에 DAY — date·slot·subject·start·end·item·module_ref·planned_qty·actual_qty·status·source·evidence·task_ref·next. `미상` = 기록 0건인 날
② 노션 DB 7종(DAY·INBOX·ACTION·TASKS·HEARTBEAT·MOCK_SEG·REVIEW) 생성, id를 `config.databases`에. **1단계에서 노션 쓰기 「없음」이면 이 블록은 sqlite까지만 하고 나머지는 대기함**
③ 기존 DB 9종 불변

### P1-2 · 기록 받기(소급)
① 파서 — `9/8 수 12,17 / 사문 3`. 날짜 없으면 오늘. 여러 날 동시. **같은 날 재입력 = 갱신**
② 판정 — 배정에 있으면 완료(실측) / 없던 것이면 대체 / 「미완」 명시만 미수행 / 언급 없는 배정 슬롯은 완료(추정) / 기록 0건은 미상
③ `rules.json` — 기록 문법·실모·대체 + 사문 줄임말 + 실패별 「우뢰가 할 1행동」
④ `notify.py` — 한 함수 5경로
⑤ 9/7~9/19 가상 입력 검증은 **`data\_selftest.sqlite`에서만**. 노션 쓰기 0. 결과표 출력 후 임시 DB 삭제 대신 `tmp\`로 이동

### P1-3 · 화면
① `render.py` — [F]/[R] 존 분리. **마커 없으면 만들지 말고 장애 보고**
② `home.py`·`dashboard.py`·`watch.py`의 노션 렌더를 흡수하고, **1단계 grep으로 찾은 호출부(bat·XML·hooks) 전부를 `render.py` 하나로 교체**. 셋은 `scripts\_retired\`로 이동
③ ACTION 뷰 정렬 = 우선순위 순
④ 상태 둘째 칸 = 「저장함 n · 이번 주 N 중 k」. 「적체」 금지
⑤ ERRORLOG·국어 `영역`→`단원` — **`RENAME COLUMN`**(속성 이름 변경이라 값이 안 지워진다). 실행 전 그 속성을 참조하는 수식·롤업·뷰 필터를 grep하고, 행 수와 비어 있지 않은 값 수를 센 뒤, 실행 후 같은 수인지 대조. 다르면 즉시 보고
⑥ **여기서 나를 불러라** — [F] 존 항목을 하나씩, 내가 「됐다」 할 때마다 다음

### P1-4 · 자동 처리
① `orch.py tick` — **LLM 호출 0**, 규칙 분류만. 0매치는 미분류로 ACTION
② 모든 자동 결정에 `결정` JSON(intent·rule_id·tool·channel·model·effort·reason·verify·undo). **undo 빈 결정은 실행 금지**
③ `.claude/commands/burn.md` → `/burn` = `python orch.py tick --all`
④ 압축어 확정 13건 사전 착지(⑥~⑩ 생성 안 함)
⑤ 이제 `UTO_04_틱` `/ENABLE` → 시험 입력 1건으로 15분 안 반영 확인

### P1-5 · 복습 저장함
① 예제 cap 제거, 대기 전량 `REVIEW`로 이관 — **버리는 것 0. ERRORLOG 행은 읽기만**
② T1 확신오답 / T2 △오답 / T3 △정답 / T4 종결확인
③ 점수 = `100·계층 + 40·min(모듈빈도28,5)/5 + 30·간격적합도 + 20·회차 − 25·최근처리`. D-61 기준 창 T2 7~13일 · T1 2~5일
④ 같은 모듈 연속 2개 금지. 같은 모듈 3개 이상 + 학습중·측정불가면 묶어 비교 블록
⑤ 30일 초과 항목 주 1건 강제 편입
⑥ 주간 계획에 「이번 주 예제 N행」 select — 제안 min(가용분÷평균분, 지난주×1.2) · 무응답 = 지난주 실제
⑦ 영어 8행(9평 21·32·33·34·36·37·41·42)을 pool 없이 원문항으로 T1/T2 편입. 과목별 가중 금지
⑧ `config.params.review` 전 파라미터 노출 + 「첫 4주 동결」
⑨ **여기서 나를 불러라** — 상위 20건을 점수·이유와 함께

### P1-6 · 컨디션과 마무리
① 홈에 컨디션 「나쁨/보통/좋음」 1탭. 「나쁨」이면 그날 메인을 루틴으로. 「오늘 쉼」과 별개
② 「나쁨」 3일 연속이면 콜아웃 줄2에 문장 1개. 관찰만
③ 「문학 태도 확인 체크리스트」를 전 과목으로 확장해 시험 당일 압축어 1장
④ 국면 5(D-23) 리허설에 시험장 조건 4가지(시각·좌석·소음·간식)
⑤ METHOD에 옵시디언 볼트 `2028 수능 › 세계지리` 링크 한 줄. 동기화 금지
⑥ 국어 태도 체크리스트 §2 완성

---

## 멈춰야 하는 조건
- `run.bat test` 통과 수 감소
- 되돌릴 수 없는 삭제·덮어쓰기가 필요해짐
- 스케줄러 변경이 승인 거부로 실패
- 노션 쓰기가 승인 요구로 거부 — **그 턴 STATE-LOG에 한 줄**(09-16 미기록으로 두 턴이 허위 전제로 갔다)
- 사용량 한도에 걸리면 → 진행 중 블록을 깨끗한 지점에서 끊고 `build_progress.json`에 「중단」으로 기록. 우뢰가 크레딧을 충전한 뒤 같은 프롬프트를 다시 넣으면 그 지점부터 잇는다

## 나를 불러야 하는 지점은 네 곳
P0-1 ④(파일 삭제) · P0-2 ⑦(UAC·스케줄러) · P1-3 ⑥([F] 존) · P1-5 ⑨(상위 20건). 그 외에는 진행.

## 끝낼 때
① `run.bat test` ② git 커밋 ③ STATE-LOG 한 줄 ④ 블록별 상태표(완료/미완/막힘 + 이유) ⑤ 다음 재개 지점 한 줄

이번 세션은 P0-1부터 P1-6까지 한 번에 끝내는 것이 목표다. 토요일 18:30 주간 턴과 겹칠 때만 P0-2를 그 뒤로 미룬다.
