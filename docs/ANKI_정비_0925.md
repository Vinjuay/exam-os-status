# Anki — 세계지리 v2 publish · 2026-09-25 (목) · D-55

**Trigger** 우뢰 「지금 로컬로 옮 이어서해줘」 — run `docs/세계지리_교재목차_정본/00_노트북_실행지시_0923.md` on the notebook.
**Path** notebook · AnkiConnect direct (appendix §12.3-G10, no .apkg round trip). `anki_add.py` does not exist (UTO build phase 3); the instruction now says so.
**Backup** `logs/anki_backup_0925/` (collection.anki2 after sync + `세계지리_notes.json` notesInfo/cardsInfo) · scripts `tmp/geo0925/`.
**Appendix read first** (Z1) — Notion `📐 anki-card-rules 부록`: G1/G2 (content-hash GUID) govern .apkg re-import only; G10 path has no conflict.

---

## 0. Result

| Item | Result |
|---|---|
| §1 measure | 342 notes / 403 cards / 8 suspended — matches 09-24 A1 |
| §2 old-index suspend | 1 (`1788588019829` 대륙별 가축 구성 · `옛색인정지-0923`) · 23 `v1설계표에만` rows = 0 hits · CH-05-03 = 0 · regex report = 0 |
| §2 X16 absorb | 7 suspended (`X16흡수-0923`): 184087 → CH-17-11 · 184001/002/003/005/006 → CH-20-04 (+05) · 184008 → CH-20-06 |
| §2-1 tag move | 18 → `교재-09` (tag `위치판별`: 위도 10 + 지도 8지점 8) · 1 차 → `교재-22` |
| §2-2 지형 | residual 11 (after excluding `1789120293711` 카나트, a `0911_기출지점_쾨펜` 함정 card filed in 지형) · no X25-valid absorber → 3 new chain cards · 3 old suspended (`X25교체-0924`) · **8 held** |
| §3 decks | created `개념::세계화와지역이해` · `개념::쾨펜::기후대` · **`개념::인구도시-도시`** (missing in live; CH-19 target) — all on preset `세계지리 게이트` |
| §4 add | 201 (add 113 − TOC 8 skip + add-and-suspend-old 69 + update-or-add 27 all → add: none of their fronts was ever live) |
| §4 back edit | 5 (CH-20 update-back-only; note ids hand-picked because match_fronts were abbreviations; A3 09-19 had not touched them; `.hl` 우뢰 marks kept on 방글라데시·인도네시아 / 브라질) |
| §4 suspend-only | 0 (CH-14 C10 · C9 not live) · index-only 2 skipped |
| §4 preset | `세계지리 게이트` new gather order Deck → Ascending position. New/day 25 unchanged |
| After | **546 notes / 607 cards / 19 suspended** · filtered-deck search = 230 cards |

## 1. Deviations from the instruction (reasoned, recorded)

- `anki_add.py` missing → AnkiConnect `addNotes` with `allowDuplicate:false` after normalized-front check; ledger `front_hash ↔ noteId` in `anki_undo_0925_add.json`.
- Field names: live model uses `앞면/뒷면`, not `Front/Back`.
- §2-1 위도대: front/city rule found 12 (cities sit on the back). Tag `위치판별` gives exactly the documented 18.
- §2-2: residual was 12, not 11 — the extra is the 카나트 함정 card (0911 family, not 암기본 §7). Excluded.

## 2. Held for 우뢰 (§2-2 step 4 · 0924-14)

Old 지형 notes whose leaves are not in the new chain cards — not suspended:

| Old note | Missing leaf |
|---|---|
| 184140 순상 사례 | 마우나케아산 (하와이 is in) |
| 184141 성층 3대 사례 | 후지산 · 마욘산 · 탐보라산 · 섭입 경계 |
| 184145 칼데라호 2 | 크레이터호 · 로토루아호 |
| 19910 화산 형태 = 점성 | 안산암 · 섭입 (점성·현무암 in) |
| 184147 탑카르스트 2 | 구이린 · 하롱베이 |
| 184149 카렌 2 | 석림 · 그랑칭기 |
| 184151 석회동굴 | 포스토이나 |
| 184154 본고장 | 슬로베니아 |

**Resolved 우뢰 09-25 「둘다 가」**: ① leaves appended as one `.sup` 사례 line on the 화산 (`1790266521248`) and 용식 (`1790266521256`) chain cards; the 8 notes above suspended (`X25교체-0924`, undo in `_x25.json`). Totals now 546 / 607 / **27 suspended**. ② 연상 pairs kept.

Live 연상 overlap pairs (not suspended): 「화산」 4갈래 ↔ new 화산 □ · 「카르스트」 2갈래 ↔ new 용식 □+□ / 침전 □.
No `_v0924.png` on any old 11 note (maps live on the 연상 cards).

## 3. §4-1 filtered deck (done) · the pass (scheduled 9/30 Wed)

- Created via the **Anki MCP add-on** (`127.0.0.1:3141`, tool `filtered_deck`) — AnkiConnect has no create action. Deck `세계지리 전수검사` id 1790268453535 · 230 cards · order added · `reschedule=false` (preview).
- Limit: `previewGoodSecs=0` cannot be set by either API → a Good answer returns the card home. Preview does not change card state, so `filtered_deck rebuild` restores all 230 (tested: 1 Good left, rebuild → 230).
- One-card test 0925: 2 answers logged (ease 2, 3) → revlog OK. Baseline reset after the test (`logs/anki_check_0925.json` `start_ms`). Synced.
- 우뢰 09-25: the full pass runs **9/30 (수)** — queued in 📥 요청 원장 「미결」 for the 9/26 weekly turn. After the pass: `python tmp/geo0925/s41_check.py --rebuild` (first answer only: ease 1 = 까먹음, 3/4 = 앎) → report forgotten cards by chapter → sync.
- Pending: Notion appendix 「실행 기록 — 2026-09-25」 after the pass.
- Found, not changed: `연상::경제지리` · `연상::지형` still on the Default preset.

## Undo JSON

`anki_undo_0925_suspend.json` · `_tags.json` · `_x25.json` · `_decks.json` · `_add.json` · `_update.json` · `_preset.json`
