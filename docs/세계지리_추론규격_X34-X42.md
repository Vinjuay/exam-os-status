# 추론 규격 — X34 ~ X40 (우뢰 09-22 승인으로 신설)

## 왜 생겼나

기존 규격 **X27** 은 「근거가 없으면 `-`」 였고 **§9-P6** 은 창작 금지였다. 그 아래서 개수 125건이 `-` 로 남았다.
우뢰가 09-22 에 원인을 밝혔다 — **「이승헌이 말로만 목차를 설명했기 때문에 필기해둔 것은 보낸 게 다다.
커버 안 되는 부분은 너가 추론해서 목차를 만들어야 한다.」**

곧 원전은 더 안 나온다. `-` 를 고집하면 검산 장치가 영원히 반쪽이다. 그래서 추론을 **허용**하되,
추론이 원전으로 둔갑하는 것은 여전히 **금지**한다. 이 절이 그 경계다.

## 조항

- **X34 — 개수 출처는 넷이다.** `원전명시` · `실측` · **`추론`** · `없음(-)`.
  추론은 원전이 침묵한 자리를 메운다. 메운 자리는 **반드시 `추론` 으로 적힌다.**
  추론을 `실측` 이나 `원전명시` 로 적는 것은 이 프로젝트에서 가장 무거운 위반이다 — 검산 장치 전체를 못 믿게 만든다.

- **X35 — 추론에는 근거 계층이 있다.** 강한 순서:
  1. **㉠ 교재 인쇄 구조** — 장(01~26) · 절(①②③) · 항((1)(2)) 번호. 이승헌이 말을 안 했어도 지면은 인쇄돼 있다. **가장 강하다.**
  2. **㉡ 교육과정 성취기준** — 2015 개정 세계지리 대단원·성취기준. 강사 순서는 바뀌어도 이건 안 바뀐다.
  3. **㉢ 수능·평가원 기출의 출제 축** — 실제로 물어 온 것.
  4. **㉣ 필기의 부분에서 전체 복원** — 개수만 있고 이름이 없을 때(「Af 2」·「고기산맥 5」), 그 개수에 맞는 이름을 ㉠~㉢ 으로 채운다.
  5. ㉤ 일반 지리 지식.
  **㉤ 단독 추론은 금지한다.** 최소 ㉠~㉣ 중 하나에 걸려야 추론으로 인정된다. 어디에도 안 걸리면 `-` 로 남긴다.

- **X36 — 추론 개수는 카드에 표시한다.** 뒷면 `.sup` 줄에 `추정` 과 근거 계층 기호(㉠~㉣)를 단다.
  우뢰가 「이건 잠정이다」를 **모르고** 외우면 안 된다. 표시 없는 추론 카드는 발행하지 않는다.

- **X37 — 한 카드 안에서 원전과 추론을 섞지 않는다.** 사슬의 일부만 추론이면 사슬을 가르거나,
  추론 노드에 표시를 붙여 어디까지가 원전인지 눈에 보이게 한다.

- **X38 — 추론은 개정을 전제한다.** 원전이 나중에 나오면 **X26**(뒷면만 수정 · GUID 유지)으로 갈아끼운다.
  그래서 추론 카드는 **잠그지 않는다**(X33 과 같은 취지). 갈아끼울 것을 잠그면 갈아끼울 수 없다.

- **X39 — 충돌하면 원전이 이긴다.** 추론이 필기·교재와 어긋나면 **추론을 버린다.**
  추론으로 원전을 덮어쓰지 않는다. 원전이 이상해 보여도 원전이 정본이다.

- **X40 — 단원마다 추론 비율을 적는다.** 「원전 N / 추론 M」. 추론이 절반을 넘는 단원은 **「추론 우세」** 로 표시한다.
  우뢰가 어느 단원을 이승헌에게 직접 확인해야 하는지 한눈에 보게 하기 위해서다.

## 판정 예

| 자리 | 원전이 준 것 | 판정 |
|---|---|---|
| 「인구구조 4」 + 묶음 4노드 | 개수·이름 모두 | `원전명시 4` |
| 「Af 2」 + 북부/사하라이남 2덩어리 | 개수·이름 모두 | `원전명시 2` |
| 「고기산맥 5」, 이름 없음 | 개수만 | `원전명시 5` + 이름은 **㉣ 추론** (표시 필수) |
| 교재 「11 건조」 장, 필기 없음 | 장 제목만 | 절 구조는 **㉠ 추론** |
| 도시 단원 중분류 수 | 아무것도 없음 | 교재 「19 도시」 절 구조로 **㉠ 추론**, 그것도 안 되면 `-` |

---

# X41 · X42 — 우뢰 09-22 2차 지시로 신설

우뢰 지시 원문: **「항상 최신 자료를 기준으로 하되 지금처럼 범위가 없는 경우만 예외적으로 이전 자료 허용.
교재 기준으로 해.」**

## X41 — 최신이 이긴다

같은 것을 두 자료가 다르게 말하면 **최신 자료가 정본**이다. 이전 자료는 최신이 **그 범위를 다루지 않을 때만** 쓴다.

### 자료 서열 (위가 최신·강함)

| 순위 | 자료 | 성격 | 무엇에 이기나 |
|---|---|---|---|
| **1** | **우뢰의 강의 현장 필기** — 교재 여백 필기 **· EBS 수능완성 표시 누적**(09-23 우뢰 결정 5 · **p.49·p.50 파란 펜도 우뢰 필기** — 09-23 우뢰 본인 확인) | 이승헌 강의 중 우뢰가 교재·EBS 책 **위에 덧쓴** 것 | **값**(개수·정정·강조)에서 전부 이긴다. EBS 필기는 값의 근거이되 교재의 절 구조를 바꾸지는 않는다 |
| **2** | **기승전결 교재** (2027학년도 인쇄) | 이승헌의 인쇄 원전 | **구조**(목차 뼈대)의 기준. 필기 없는 곳을 덮는다 |
| 3 | 개념망 v1 (이승헌 원 필기 전사) · **이승헌 코멘트 누적**(09-23 우뢰 결정) | 이승헌 발화의 전사 | 1·2가 침묵할 때만 |
| 4 | 마스터본 · 암기본 | **AI 편집본** — 원전이 아니다 | 1~3이 없을 때만. 구조 근거로 쓰지 않는다 |
| 5 | REAL-S 통계 정본 | 수치 보조 | 수치에 한해 |

**표시 누적 문서 안의 두 서열** (09-23 명문화) — 「EBS 수능완성 표시 누적」 같은 판독 문서에는 두 층이 섞여 있다.
서열 1은 **우뢰가 표시한 대상**에만 붙는다. 밑줄·동그라미·숫자가 놓인 지면의 자리가 그 대상이다.
판독 AI가 그 표시를 읽고 쓴 정리(§8 판독 정리 등)는 **서열 4**다(AI 편집본). 그래서 밑줄이 있는 칸의 값은 원전이다.
여러 칸을 한 축으로 묶는 읽기(예: 「생산량 ≠ 매장량 ≠ 수출량」)가 §8에서 왔다면 그 읽기는 추론이다(X34 · X37).

**필기가 교재를 이기는 이유** — 필기는 교재 지면 위에 나중에 쓰였다. 「유럽 3→**4**」처럼 교재의 수를
강의에서 고친 흔적이 그 증거다. 화살표가 가리키는 쪽이 최신이다.

**교재가 필기를 이기는 자리** — 필기가 **침묵하는 범위**. 필기는 우뢰가 적은 것이 전부이고(우뢰 09-22 선언),
적지 않은 장이 훨씬 많다. 거기서는 교재의 인쇄 구조가 유일한 원전이다.

### 「범위가 없는 경우」의 뜻

최신 자료가 그 주제를 **아예 다루지 않는 것**을 말한다. 최신 자료가 다루는데 내용이 마음에 안 들어
이전 자료를 끌어오는 것은 **금지**다. 그것은 최신 우선의 위반이지 예외가 아니다.

## X42 — 기준 색인은 교재다

목차와 개수의 판정 기준은 **교재의 인쇄 구조**다.

```
L0 세계지리  >  L1 PART(Ⅰ~Ⅶ)  >  L2 장(01~26)  >  L3 절(①②③)  >  L4 항((1)(2)) · 사슬  >  노드
```

- 마스터본의 「원리 L/M/N」 같은 **AI 편집본이 만든 구획은 목차의 층이 될 수 없다.** 그것은 보조 설명이다.
- 기존 14단원(`U-세지-01`~`14`)은 **폐기하지 않는다** — Anki 덱이 거기 걸려 있다.
  ([정정 09-23] 이전 판은 「Anki 덱과 273장 카드」라 적었다. 273은 v1 설계표의 카드 수이고 발행된 수가 아니다.
  근거는 00_정지대체표 unresolved[0], A4·A5 미실행이다. 실제 수는 노트북 실행지시 §1 계량으로 확정한다.)
  교재 장마다 「어느 덱에 들어가는가」를 적어 **대응표**로 잇는다. 색인은 교재, 수납은 덱이다.
- 교재에 있는데 기존 14단원에 없는 것(예: 「08 오세아니아」)은 **교재 쪽이 옳다.** 채워 넣는다.
- 교재가 두 주제를 한 장에 묶었으면(예: 「16 화산·카르스트·해안」) **묶인 채로 센다.** 풀어 헤치지 않는다.


---

# 09-23 우뢰 결정 기록

| # | 결정 | 반영 |
|---|---|---|
| 1 | G0 적체 분모 — main 에 머지된 **㉰(분모 없이 「적체 N」)** 확정 | 대시보드 쪽은 main(PR #15) 그대로 |
| 2 | PART Ⅰ 01~08 을 **신설 덱 하나**(`세계지리::개념::세계화와지역이해`)에 — U-세지-01 L1 검산 **4 → 8** | 발행 목록 덱 경로 · UNIT 행 |
| 3 | 기후 10~13 장 기후대 특징 카드는 **하위 덱 `쾨펜::기후대` 신설** | 발행 목록 덱 경로 |
| 4 | 앞면이 옛 강 번호·AI 구획인 기존 카드 — **㉮ 지금 일괄 정지하고 대체 카드 발행** | 정지·대체표 → 노트북 실행 |
| 5 | 자료 서열 — **EBS 수능완성 표시 누적 = 서열 1** · **이승헌 코멘트 누적 = 서열 3** | 위 X41 표 · EBS 근거 노드 재판정 |
| 6 | **「장으로 분류까지만 해 절은 너무 세부적임」** — 목차 카드는 장까지 | 아래 X30′ · 장 안 목차 카드 가르기 |

**결정 5 의 파급** — 09-22 워크플로우의 에이전트들은 EBS 필기를 「교재 여백이 아니라서 서열 1 로 세지 않는다」며 서열표 밖이나 하위로 뒀다.
그 판정은 폐기다. EBS 를 근거로 쓴 노드 47개(13장)를 새 서열로 다시 판정한다.

재판정 결과(적대적 검증 통과) — 6장에서 노드 15개와 카드 mark 2개가 바뀌었다. 14·20·22·24·26은 basis_tier를 필기로 올리거나 status를 확정으로 올렸다. 21 「생산량 ≠ 매장량 ≠ 수출량」은 반대로 **내렸다**(실측·확정 → 추론 ㉣·검증필요). 필기가 대는 것은 p.72의 생산량·매장량 두 항뿐이고, 수출량 칸은 표시 누적 §8(판독 AI 정리)과 p.49 파란 펜(표시 주체 미상)에 기대기 때문이다. 나머지 7장(08·11·12·17·18·23·25)은 바뀌지 않았다.

▸ [해소 09-23] 파란 펜 답변 근거 — 우뢰가 09-23에 p.49·p.50 파란 펜을 본인 필기로 확인했다(서열 1). 위 문단의 「표시 주체 미상」은 낡은 문구다.
p.49에서 밑줄이 그어진 자리는 원전이다. 밀 수출량 오스트레일리아 14.7% · 옥수수 수출량 칸 · 쌀 생산량 중국 26.0%의 식량 자원 표 세 곳이다.
그러나 세 표를 「≠ 축」 하나로 묶는 읽기는 여전히 §8 판독 정리(서열 4)에서 왔다. 그래서 21 「생산량 ≠ 매장량 ≠ 수출량」은
**추론 ㉣ · 검증필요를 유지**하고, 근거란의 「주체 미상」만 「우뢰 필기 p.49」로 바꾼다(X34 — 추론이 섞이면 실측 불가 · X37).
CH-21 nodes[25] evidence·inferred_parts가 그렇게 고쳐졌다. 20·22장 곡물 노드의 p.49·p.50 근거에도 「우뢰 필기 · 서열 1」 주석이 붙었다.
두 장 모두 값·count_kind는 그대로다(CH-20 · CH-22 applied_0923b). 규칙은 X41 「표시 누적 문서 안의 두 서열」로 명문화했다.

## X30′ — 목차 카드는 장까지 (09-23 결정 6)

```
목차 카드  =  L0(세계지리 → PART 6)  +  L1(PART → 장 8·6·2·3·2·5)   ← 여기까지
장 안      =  사슬 카드(X15)가 맡는다. 절·항 구조는 카드로 묻지 않는다
```

- v1 X30(「목차 카드는 L0·L1·L2 세 층」)을 대체한다. **장 안의 절(①②③)·항((1)(2)) 개수와 순서를 묻는 카드는 만들지 않는다.**
- 절 구조는 추론이 대부분이라(26장 모두 절 수가 추론) 검산값으로 외우게 하면 추론을 굳힌다. 절은 CH 문서의 **정리 틀**로만 남는다.
- 장 안 「목차」 카드는 셋으로 가른다. ① 절·항 구조를 묻는 카드는 **뺀다**(`held_gate`, 사유 「우뢰 09-23 목차는 장까지」). ② 지리 내용의 묶음(권역·축·원인 □개)을 묻는 카드는 kind를 **사슬**로 바꿔 살린다. ③ 앞면에 절 표지만 붙은 내용 카드는 표지를 걷어 **다시 쓴다**.
- 이승헌이 부른 개수(인구구조 4 · 건조 지형 12 · 라 Am 2 · Af 2 · 유럽 3→4 등)는 어느 경우에도 살린다. 절 구조가 아니라 내용 묶음이기 때문이다.
- `00_검산표.md`의 L2 열(장 = 절, 합 71)은 **참고**로 내린다. 검산 대상이 아니다.

